package com.pintking.api

import org.junit.jupiter.api.Test

class PintKingApplicationTest {

    @Test
    fun `project compiles and test target runs`() {
        // This test verifies the project is set up correctly.
        // It will be replaced with real tests as features are built.
        assert(true)
    }
}
